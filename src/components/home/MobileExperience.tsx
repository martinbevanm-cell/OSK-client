/**
 * Deprecated — replaced by `GetOurApp`. This file is kept as an empty
 * stub so removing it is a non-event for tooling; the next housekeeping
 * pass can delete it. Re-exports GetOurApp under the legacy name so any
 * stale imports keep working.
 */
export { GetOurApp as MobileExperience } from './GetOurApp';
